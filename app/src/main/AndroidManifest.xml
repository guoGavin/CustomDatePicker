<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.gavinguo.customdatepicker" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".DatePickerSamples"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DialogSample"
            android:theme="@style/SampleTheme"/>
        <activity android:name=".LayoutSample"
            android:theme="@style/SampleTheme"/>
        <activity android:name=".MyDialogSample"
            android:theme="@style/SampleThemeMy"/>
        <activity android:name=".MyLayoutSample"
            android:theme="@style/SampleThemeMy"/>

    </application>

</manifest>
